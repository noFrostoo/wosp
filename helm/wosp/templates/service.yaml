apiVersion: v1
kind: Service
metadata:
  name: {{ include "wosp.fullname" . }}
  labels:
    {{- include "wosp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wosp.selectorLabels" . | nindent 4 }}
